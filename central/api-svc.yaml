apiVersion: v1
kind: Service
metadata:
  name: api-svc
  labels:
    app: special-seminar-api
spec:
  type: NodePort
  ports:
  - port: 50051
    targetPort: 50051
    name: grpc
  selector:
    app: special-seminar-api